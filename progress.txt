================================================================================
MASTER UTM GENERATOR — PROGRESS LOG
================================================================================

── Feature 1: Project Scaffolding & Design System ── COMPLETED
   Date: 2026-02-17

   What was done:
   - Initialized Next.js 15 project (TypeScript, Tailwind v4, ESLint, App Router, src/ dir)
   - Installed dependencies: lucide-react, class-variance-authority, clsx, tailwind-merge
   - Initialized shadcn/ui (New York style, 10px border radius)
   - Added shadcn components: button, card, input, label, sheet, tooltip
   - Configured design tokens in globals.css:
     • Primary #248234, Dark BG #15250E, Light BG #F4F7FA
     • Custom Tailwind colors: dark-bg, light-bg, green-primary
     • All shadcn CSS variables mapped to green theme
   - Configured fonts: DM Serif Display (headings) + Inter (body)
   - Built app shell layout:
     • Sidebar (64px collapsed, 220px on hover, tooltips, active route highlight)
     • Mobile bottom nav (visible <768px)
     • 3 nav items: Organic & DMs, Ads, Settings
   - Created route stubs:
     • / → redirect to /organic
     • /organic, /ads — placeholder pages
     • /settings → redirect to /settings/values
     • /settings/values, /settings/connections — placeholder pages
   - Themed button variants: primary (green bg), outline (green border), destructive (red)
   - Build passes with zero errors

   Files created/modified:
   - src/app/globals.css
   - src/app/layout.tsx
   - src/app/page.tsx
   - src/app/organic/page.tsx
   - src/app/ads/page.tsx
   - src/app/settings/page.tsx
   - src/app/settings/values/page.tsx
   - src/app/settings/connections/page.tsx
   - src/components/layout/sidebar.tsx
   - src/components/layout/mobile-nav.tsx
   - src/components/ui/button.tsx (themed outline variant)
   - src/lib/utils.ts (generated by shadcn)
   - components.json (shadcn config)

   Tests: src/components/layout/sidebar.test.tsx (7 tests)
          src/components/layout/mobile-nav.test.tsx (6 tests)

── Feature 2: localStorage Storage Layer ── COMPLETED
   Date: 2026-02-17

   What was done:
   - Created TypeScript interfaces: UtmLink, UtmValue, UtmParameter,
     DataConnection, StorageAdapter
   - Created storage CRUD module with namespaced keys:
     • utm-generator:links, utm-generator:values, utm-generator:connections
   - generateId() using crypto.randomUUID()
   - Links CRUD: getLinks(filters?), addLink(data), deleteLink(id)
     • getLinks supports search (url/campaign/source) and sort (asc/desc by createdAt)
   - Values CRUD: getValues(parameter?), addValue(data) with dedup, deleteValue(id)
   - Connections CRUD: getConnections(), saveConnection(data), deleteConnection(id)
   - All functions accept optional StorageAdapter for testability
   - JSON serialization with try/catch — corrupted data returns empty arrays
   - dispatchEvent(StorageEvent) after writes for cross-tab sync
   - Created useStorageSync(key, callback) React hook for cross-tab sync
   - Installed vitest, @testing-library/react, @testing-library/jest-dom, jsdom
   - Created vitest.config.ts with path aliases and jsdom environment
   - Build passes with zero errors

   Files created:
   - src/lib/types.ts — TypeScript interfaces
   - src/lib/storage.ts — localStorage CRUD wrapper
   - src/hooks/use-storage.ts — cross-tab sync hook
   - vitest.config.ts — Vitest configuration
   - vitest.setup.ts — test setup (jest-dom matchers + cleanup)

   Tests: src/lib/storage.test.ts (25 tests)

── Feature 3: Snake_case Auto-Conversion Utility ── COMPLETED
   Date: 2026-02-17

   What was done:
   - Created toSnakeCase(input) utility function
     • Lowercases all characters
     • Handles camelCase and PascalCase boundaries (inserts underscores)
     • Handles consecutive uppercase (acronyms like HTTP)
     • Replaces spaces, hyphens, periods, and special characters with underscores
     • Collapses consecutive special characters into single underscores
     • Strips leading/trailing underscores
     • Returns empty string for empty input
   - 15 unit tests covering all edge cases
   - Build passes with zero errors

   Files created:
   - src/lib/utils/to-snake-case.ts — toSnakeCase utility
   - src/lib/utils/to-snake-case.test.ts — 15 tests

── Feature 4: Toast Notification System ── COMPLETED
   Date: 2026-02-17

   What was done:
   - Installed shadcn sonner component (wraps sonner toast library)
   - Customized Toaster component:
     • Removed next-themes dependency (light-only app)
     • Hardcoded theme="light"
     • Position: bottom-right
     • Max visible toasts: 3
     • Auto-dismiss: 4 seconds
     • Close button enabled
     • Themed toast types per PRD design tokens:
       - Success: #F4FFF0 bg, #004D23 text (light-green/deep-green)
       - Error: red-50 bg, red-900 text
       - Warning: #F4FFCC bg (yellow-tint)
       - Info: #EFF0F6 bg (light-grey)
     • Icons: CircleCheck, OctagonX, TriangleAlert, Info (lucide-react)
   - Added <Toaster /> to root layout
   - Build passes with zero errors, all 62 tests pass

   Files created/modified:
   - src/components/ui/sonner.tsx — themed Toaster component
   - src/app/layout.tsx — added Toaster to layout

   Tests: src/components/ui/sonner.test.tsx (9 tests)
          - Toaster renders, positioned bottom-right, light theme
          - Success/error/warning/info toasts with correct CSS classes
          - Close button present, multiple toasts stack

   Usage: import { toast } from "sonner"
          toast.success("Link generated!")
          toast.error("Invalid URL")
          toast.warning("Approaching storage limit")
          toast("Info message")

── Feature 5: UTM Value Management — Manual Entry ── COMPLETED
   Date: 2026-02-17

   What was done:
   - Updated UtmValue type to match PRD Section 5.2:
     • Added ManageableParameter type: 'utm_campaign' | 'utm_term' | 'utm_content'
     • Added label, source, sourceRef fields to UtmValue
     • Changed parameter field to use ManageableParameter
   - Updated storage layer:
     • getValues() accepts ManageableParameter instead of UtmParameter
     • Updated all storage tests with new fields
   - Built Values Management page (/settings/values):
     • Page heading + subtitle
     • Three ValueSection components (Campaign, Term, Content)
   - Created ValueSection component:
     • Add form with input, snake_case preview, "Add" button (supports Enter key)
     • Values list showing label + snake_case value + Trash delete button
     • Empty state message
     • Toast notifications for add, delete, duplicate errors
   - Installed @testing-library/user-event dev dependency
   - All 70 tests pass, build succeeds with zero errors

   Files created:
   - src/components/settings/value-section.tsx — ValueSection component
   - src/components/settings/value-section.test.tsx — 8 tests

   Files modified:
   - src/lib/types.ts — ManageableParameter type, updated UtmValue
   - src/lib/storage.ts — ManageableParameter import
   - src/lib/storage.test.ts — updated test data
   - src/app/settings/values/page.tsx — replaced stub with full UI

   Tests: src/components/settings/value-section.test.tsx (8 tests)
          - Empty state, heading, snake_case preview, add/delete values
          - Duplicate error toast, disabled button, Enter key support

── Feature 6: UTM Link Generator Form ── COMPLETED
   Date: 2026-02-17

   What was done:
   - Created UTM framework config module (src/lib/utm-config.ts):
     • Channel types: Organic Social, Blog, Warm DMs, Cold DMs
     • Platform configs with utmSource and postFormats per platform
     • getPlatformsForChannel() — returns filtered platforms per channel
     • getUtmMedium() — derives utm_medium from channel type
     • composeUtmContent() — builds utm_content (format-hook or blog title)
     • buildUtmUrl() — smart URL builder (? vs & separator, encodes params)
     • isValidUrl() — validates URL with http/https protocol
   - Built UTM Generator Form component (slide-in Sheet drawer):
     • Base URL input with URL validation
     • Channel Type radio group (Organic Social, Blog, Warm DMs, Cold DMs)
     • Platform dropdown filtered by channel (Google auto-locked for Blog)
     • utm_source & utm_medium shown as read-only Badges
     • utm_campaign searchable input with datalist from stored values
     • utm_term optional searchable input with datalist
     • Post Format dropdown (hidden for Blog), filtered by platform
     • Content Hook / Blog Title input (label changes per channel)
     • utm_content computed read-only display
     • Live URL preview with param breakdown
     • Tooltips on every field per PRD spec
     • Full validation (required fields, URL format, min 2 chars)
     • Auto-saves new campaign/term values (source: 'auto') on generate
     • Cancel resets and closes drawer
   - Updated Organic & DMs page:
     • "Generate UTM Link" button opens right-side Sheet drawer
     • Last generated link shown in result card (placeholder for Feature 7)
     • Empty state placeholder for table (Feature 8)
   - Installed shadcn components: select, badge
   - All 105 tests pass, build succeeds with zero errors

   Files created:
   - src/lib/utm-config.ts — UTM framework data and helper functions
   - src/lib/utm-config.test.ts — 22 tests (platforms, medium, content, URL building, validation)
   - src/components/organic/utm-generator-form.tsx — Generator form component
   - src/components/organic/utm-generator-form.test.tsx — 13 tests
   - src/components/ui/select.tsx — shadcn Select component
   - src/components/ui/badge.tsx — shadcn Badge component

   Files modified:
   - src/app/organic/page.tsx — replaced stub with Sheet drawer + generate button

   Tests: src/lib/utm-config.test.ts (22 tests)
          - Platform configs, medium derivation, content composition
          - URL building (smart separator, encoding, empty values)
          - URL validation (https, http, invalid, empty)
          src/components/organic/utm-generator-form.test.tsx (13 tests)
          - Renders fields, validation errors, URL validation
          - Channel selection, Blog auto-config, badge display
          - Snake_case preview, cancel callback, live preview
          - Full blog generation flow, auto-save values

── Feature 7: Result Card & Copy Functionality ── COMPLETED
   Date: 2026-02-17

   What was done:
   - Created ResultCard component:
     • Light green background (#F4FFF0) per design tokens
     • Full generated URL in monospace with word-break
     • Platform badge (colored pill) from utm_source
     • Formatted timestamp (e.g., "Feb 17, 2026 at 10:30 AM")
     • UTM param breakdown (each on its own line)
     • Only shows params that have values (hides empty optional params)
     • Copy URL button — copies to clipboard, shows checkmark + "Copied!" for 2s
     • Success toast: "URL copied to clipboard"
     • Dismiss (X) button closes card
   - Integrated into Organic page:
     • Card appears after drawer closes post-generation
     • Generating another link replaces previous card
     • Card is not persisted — disappears on page refresh
   - Also renamed sidebar title: "UTM Gen" → "Get Levrg - Master UTM Generator"
   - All 114 tests pass, build succeeds with zero errors

   Files created:
   - src/components/organic/result-card.tsx — ResultCard component
   - src/components/organic/result-card.test.tsx — 9 tests

   Files modified:
   - src/app/organic/page.tsx — integrated ResultCard
   - src/components/layout/sidebar.tsx — updated title text
   - src/components/layout/sidebar.test.tsx — updated title assertion

   Tests: src/components/organic/result-card.test.tsx (9 tests)
          - Renders URL, platform badge, timestamp, UTM breakdown
          - Hides empty optional params
          - Copy to clipboard + success toast + "Copied!" state
          - Dismiss callback, light green background

── Feature 8: Master Spreadsheet View — Organic & DMs ── COMPLETED
   Date: 2026-02-17

   What was done:
   - Installed @tanstack/react-table v8
   - Installed shadcn alert-dialog component (for delete confirmation)
   - Built LinksTable component with TanStack Table:
     • Columns: Full URL, Source, Medium, Campaign, Term, Content, Date, Actions
     • Full URL truncated with tooltip on hover showing full URL
     • Source shown as Badge, optional fields show "—" when empty
     • Column header sorting (click to toggle asc/desc), default newest first
     • Search input (debounced 300ms) — searches across all UTM columns + URL
     • Active search filter shown as removable chip
     • Group-by dropdown: None, utm_source, utm_medium, utm_campaign, utm_term, utm_content
     • Grouped rows: collapsible sections with row count badges
     • Row actions: Copy URL (icon + clipboard + toast), Delete (icon + confirmation dialog)
     • Delete confirmation: AlertDialog with cancel/confirm
     • Pagination: 25 rows per page, Previous/Next buttons, page count display
     • Empty state: "No UTM links generated yet" with call to action
     • Horizontal scroll on mobile for responsive behavior
   - Integrated into Organic page:
     • Table replaces the old placeholder
     • refreshKey prop triggers reload after link generation
     • New rows appear at top immediately after generation
   - Restored custom outline button variant after shadcn overwrite
   - All 124 tests pass, build succeeds with zero errors

   Files created:
   - src/components/organic/links-table.tsx — TanStack Table component
   - src/components/organic/links-table.test.tsx — 10 tests
   - src/components/ui/alert-dialog.tsx — shadcn AlertDialog component

   Files modified:
   - src/app/organic/page.tsx — integrated LinksTable with refreshKey
   - src/components/ui/button.tsx — restored custom outline variant

   Tests: src/components/organic/links-table.test.tsx (10 tests)
          - Empty state, renders links, column headers
          - Missing optional fields show dashes
          - Search input, refreshKey reloads data
          - Copy to clipboard + toast
          - Delete confirmation dialog, delete with confirmation
          - Group-by dropdown

── Feature 9: UTM Value Management — Google Sheets Connection ── COMPLETED
   Date: 2026-02-17

   What was done:
   - Updated DataConnection type: added lastSynced, status fields, narrowed type union
   - Added storage helpers: updateConnection(), deleteValuesBySource()
   - Created .env.example with GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, NEXT_PUBLIC_APP_URL
   - Google OAuth helper module (src/lib/google/auth.ts):
     • getAuthUrl() — builds OAuth consent URL with spreadsheets.readonly scope
     • exchangeCode() — exchanges auth code for access + refresh tokens
     • refreshAccessToken() — refreshes expired access tokens
     • getValidAccessToken() — auto-refreshes if needed, reads from HTTP-only cookie
     • setTokenCookie() / parseTokenCookie() / clearTokenCookie() — secure cookie helpers
   - Google Sheets API helper (src/lib/google/sheets.ts):
     • getSheetMetadata() — title + tab names via Sheets REST API v4
     • getColumnHeaders() — first row of a tab
     • getColumnValues() — unique values from specified columns (batchGet)
     • No googleapis npm dependency — raw fetch against REST API
   - Sheet URL parser (src/lib/google/parse-sheet-url.ts):
     • Extracts sheet ID from docs.google.com/spreadsheets/d/{ID}/... URLs
   - 6 API Routes (all read tokens from HTTP-only cookies):
     • GET /api/google/auth — redirects to Google OAuth consent
     • GET /api/google/callback — exchanges code, sets cookie, redirects back
     • GET /api/google/sheets/[sheetId] — returns sheet metadata
     • GET /api/google/sheets/[sheetId]/values — returns headers or column values
     • POST /api/google/disconnect — clears token cookie
     • GET /api/google/status — checks authentication status
   - GoogleSheetsConnection UI component with 4 states:
     • Disconnected — "Connect Google Sheets" button
     • Authenticated — Sheet URL input + Load button
     • Configuring — Tab selector + column mapping dropdowns
     • Configured — Connection summary, Sync Now, Disconnect (with confirmation)
     • Status indicators (green/red/grey), last synced timestamp
     • Sync: pulls values, snake_cases them, stores with source='google_sheets'
     • Disconnect: clears cookie + connection + sourced values
   - Updated /settings/connections page: renders component + Airtable placeholder
   - Build passes with zero errors, all 150 tests pass

   Files created:
   - .env.example
   - src/lib/google/auth.ts
   - src/lib/google/sheets.ts
   - src/lib/google/parse-sheet-url.ts
   - src/app/api/google/auth/route.ts
   - src/app/api/google/callback/route.ts
   - src/app/api/google/sheets/[sheetId]/route.ts
   - src/app/api/google/sheets/[sheetId]/values/route.ts
   - src/app/api/google/disconnect/route.ts
   - src/app/api/google/status/route.ts
   - src/components/settings/google-sheets-connection.tsx
   - src/lib/google/parse-sheet-url.test.ts — 8 tests
   - src/lib/google/sheets.test.ts — 7 tests
   - src/components/settings/google-sheets-connection.test.tsx — 5 tests

   Files modified:
   - src/lib/types.ts — DataConnection: added lastSynced, status, narrowed type
   - src/lib/storage.ts — added updateConnection(), deleteValuesBySource()
   - src/lib/storage.test.ts — 6 new tests for new storage functions
   - src/app/settings/connections/page.tsx — "use client", renders GoogleSheetsConnection

   Tests: 150 total (20 new)
          src/lib/google/parse-sheet-url.test.ts (8 tests)
          src/lib/google/sheets.test.ts (7 tests)
          src/components/settings/google-sheets-connection.test.tsx (5 tests)
          src/lib/storage.test.ts (+6 tests for updateConnection, deleteValuesBySource)

── Feature 10: UTM Value Management — Airtable Connection ── COMPLETED
   Date: 2026-02-17

   What was done:
   - Created Airtable API helper module (src/lib/airtable/api.ts):
     • listBases(token) — GET /meta/bases → returns { id, name }[]
     • listTables(token, baseId) — GET /meta/bases/{baseId}/tables → returns tables with fields
     • getRecords(token, baseId, tableId, fieldNames) — paginated fetch of unique values per field
   - Created 6 API routes (all read PAT from HTTP-only cookie):
     • POST /api/airtable/connect — validates PAT by listing bases, sets HTTP-only cookie
     • GET /api/airtable/bases — returns list of bases
     • GET /api/airtable/bases/[baseId]/tables — returns tables + fields for a base
     • GET /api/airtable/bases/[baseId]/tables/[tableId]/records — returns unique values per field
     • POST /api/airtable/disconnect — clears PAT cookie
     • GET /api/airtable/status — checks if valid PAT cookie exists
   - Built AirtableConnection UI component with 4 states:
     • Disconnected — PAT input (password type) + Connect button
     • Authenticated — Base selector dropdown
     • Configuring — Table selector + column mapping dropdowns (3 params)
     • Configured — Connection summary, Sync Now, Reconfigure, Disconnect
     • Same patterns as GoogleSheetsConnection: status badges, toasts, AlertDialog
   - Updated /settings/connections page: replaced Airtable placeholder with <AirtableConnection />
   - Build passes with zero errors, all 165 tests pass

   Files created:
   - src/lib/airtable/api.ts — API helper (listBases, listTables, getRecords with pagination)
   - src/lib/airtable/api.test.ts — 9 tests
   - src/app/api/airtable/connect/route.ts
   - src/app/api/airtable/bases/route.ts
   - src/app/api/airtable/bases/[baseId]/tables/route.ts
   - src/app/api/airtable/bases/[baseId]/tables/[tableId]/records/route.ts
   - src/app/api/airtable/disconnect/route.ts
   - src/app/api/airtable/status/route.ts
   - src/components/settings/airtable-connection.tsx
   - src/components/settings/airtable-connection.test.tsx — 6 tests

   Files modified:
   - src/app/settings/connections/page.tsx — replaced Airtable placeholder with AirtableConnection

   Tests: 165 total (15 new)
          src/lib/airtable/api.test.ts (9 tests)
          src/components/settings/airtable-connection.test.tsx (6 tests)

── Feature 11: Field Validation & Guidance System ── COMPLETED
   Date: 2026-02-17

   What was done:
   - Added on-blur validation for individual fields:
     • Base URL, utm_campaign, utm_term, Content Hook/Blog Title validate on blur
     • Uses per-field validateField() function for granular validation
     • Tracks touched state per field to show contextual help only for untouched fields
   - Added scroll-to-first-error on Generate click:
     • Each field section has a ref
     • On validation failure, scrolls to first errored field with smooth animation
   - Generate button visually muted when form is incomplete:
     • Computed isFormComplete checks all required fields
     • Button gets opacity-60 when incomplete, still clickable (shows all errors on click)
   - Added contextual helper text examples below fields:
     • utm_campaign: "e.g., brand_awareness, product_launch_q1"
     • utm_term: "e.g., social_proof, customer_stories"
     • Content Hook: "e.g., 5_tips_for_growth, behind_the_scenes"
     • Blog Title: "e.g., how_to_build_a_brand, ultimate_guide_seo"
     • Helper text hides when field has value or error
   - URL preview now shows ??? placeholders in the actual URL string for missing params:
     • e.g., &utm_campaign=%3F%3F%3F renders in preview URL
   - Build passes with zero errors, all 177 tests pass

   Files modified:
   - src/components/organic/utm-generator-form.tsx — added onBlur, refs, touched state,
     isFormComplete, helper text, scroll-to-error, muted button
   - src/components/organic/utm-generator-form.test.tsx — 12 new tests for Feature 11

   Tests: 177 total (12 new)
          src/components/organic/utm-generator-form.test.tsx (25 tests, was 13)
          New tests cover: blur validation (3), clear error on input, muted/active button (2),
          scroll to error, contextual helper text (4), ??? in URL preview

   Bugfix (post-feature):
   - Fixed "Can't perform a React state update on a component that hasn't mounted yet"
     error in LinksTable. Changed from useEffect-based initial load to useState lazy
     initializer: `useState<UtmLink[]>(() => getLinks())`. useEffect now only reloads
     on subsequent refreshKey changes.
   - File: src/components/organic/links-table.tsx
