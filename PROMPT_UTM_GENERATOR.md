# Master UTM Generator: SaaS Feature Completion (GSD-Optimized)

**Objective:** Complete the Master UTM Generator with a focus on precision, advanced automation, and brand-aligned UX using the GSD framework.

## 1. Workflow Mandate (GSD System)
You MUST operate within the **GSD Workflow**.
- **Resumption:** Run `/gsd-resume-work` to sync with `ROADMAP.md` and `STATE.md`.
- **Planning:** Use `/gsd-plan-phase` for the next set of features (Bulk Generation, History).
- **Design:** Use `/gsd-sketch` to explore UI variants for the dashboard. Use `/gsd-ui-review` for visual audits.
- **Code Quality:** Enforce `andrej-karpathy-skills:karpathy-guidelines` for all Next.js and logic implementation.

## 2. Core Implementation Tasks
- **Logic Completion:** Finish the UTM engine in `src/lib`. Ensure it handles all edge cases (URL encoding, custom parameters).
- **UX Polish:** Use `/ui-ux-pro-max` and `/gsd-ui-phase` to implement high-fidelity components.
- **Progress Tracking:** Keep `progress.txt` and `.planning/STATE.md` in lockstep.

## 3. Specialized Tools
- **Browser Testing:** Use `gstack:qa` to verify the generation flow in a real browser.
- **Project Strategy:** Run `gstack:/plan-ceo-review` on `PRD.md` to prioritize features for the next phase.
- **Technical Verification:** Use `/gsd-spike` for complex attribution logic experiments.

---
**Start by running `/gsd-resume-work` to identify the current milestone and pending phases.**
